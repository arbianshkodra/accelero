# Required
API_KEY=

# Optional server settings
# SERVER_PORT=8000
# DOCKER_SOCK=unix:///var/run/docker.sock
# DATABASE_PATH=./data/accelero.db

# Legacy single-stack mode (creates a "default" stack on first startup)
# For multi-stack setups, use the API instead.
REPO_URL=
REPO_USERNAME=
REPO_TOKEN=
REPO_BRANCH=
COMPOSE_PATH=
SERVICE_NAMES=

# Docker registry (optional)
DOCKER_USERNAME=
DOCKER_PASSWORD=
DOCKER_REGISTRY=
